let message;
mesage = [{data:26}];
console.log(mesage);  // this will not through any error. Because in js the undecleared variable aumatically added in window object. 
//to prevent this thing we can use use strict method on the top.